import React from 'react';
import {videoData} from '../../data/videos'

const Hero = () => {
    console.log(videoData);
    return (
        <section className="hero">

        </section>
    )
}

export default Hero
